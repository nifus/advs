(function () {
    'use strict';
    angular
        .module('backApp')
        .controller('dashboard', dashboard);

    dashboard.$inject = ['$scope', '$filter', '$q'];

    function dashboard($scope, $filter, $q) {




    }

})();